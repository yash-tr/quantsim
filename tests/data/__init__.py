# This file makes Python treat the `data` directory (within tests) as a package.
# It might not be strictly necessary if you're not importing from other test files within tests/data.
# But it's good practice for test organization.
